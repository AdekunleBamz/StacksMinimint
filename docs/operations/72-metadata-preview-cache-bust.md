# Metadata Preview Cache Bust

After metadata schema or gateway changes, reload the mint preview with a fresh browser session before release signoff.

Verification: confirm the preview image, name, and attributes match the latest metadata JSON.
