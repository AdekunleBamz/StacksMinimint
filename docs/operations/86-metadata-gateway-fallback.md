# Metadata Gateway Fallback

## Summary
Metadata previews should explain when a gateway fallback is being used.

## Checks
- Simulate primary metadata gateway failure.
- Confirm fallback URLs use supported schemes.
- Keep missing metadata distinct from slow metadata.
