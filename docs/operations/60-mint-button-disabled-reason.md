# Mint button disabled reason

Every disabled mint action should expose a specific reason.

Prefer reasons tied to form validation, wallet state, contract state, or pending transaction state.

Verification evidence: check each disabled state in the mint flow before release.
