# Contract Alias Release Note

Include active core, hub, and token contract aliases in release notes whenever deployment targets change.

Verification: match the aliases in release notes against `Clarinet.toml` and the frontend contract config.
