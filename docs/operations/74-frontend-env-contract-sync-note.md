# Frontend env contract sync note

Review frontend contract environment values before release so mint calls and explorer links target the deployed contracts.

## Checklist

- Compare frontend environment values with the release contract map.
- Rebuild previews after core or hub contract identifiers change.
