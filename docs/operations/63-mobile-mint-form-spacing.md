# Mobile mint form spacing

Review mint form spacing on narrow viewports with the wallet banner visible.

Primary mint actions should remain reachable without overlapping sticky navigation or toast messages.

Verification evidence: capture a mobile viewport screenshot during preview build verification.
