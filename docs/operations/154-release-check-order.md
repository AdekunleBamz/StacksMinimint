# Release Check Order

Run the Clarinet trait check before the frontend smoke and production build pair when preparing deployment evidence. That keeps SIP trait availability verified before the Vercel bundle output is reviewed.
