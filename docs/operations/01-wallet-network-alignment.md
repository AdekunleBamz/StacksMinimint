# Wallet network alignment

Before running mint or stake actions, confirm wallet network and frontend network are identical.
Most transaction-link and nonce confusion starts from mixed network sessions.

Capture the wallet chain selector and the app network badge in the same screenshot before retrying a failed transaction.
