# Release health dashboard note

Capture a compact health summary after each production release.

Include frontend build status, contract read status, wallet smoke status, and known follow-up owners.

Verification evidence: link the health summary from the release handoff note.
