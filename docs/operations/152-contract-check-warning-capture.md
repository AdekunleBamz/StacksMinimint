# Contract check warning capture

If `clarinet check` passes with warnings, include the warning count in release evidence.

Note whether warnings came from core, token, hub, or trait contracts.

This gives reviewers a stable baseline for future contract edits.
