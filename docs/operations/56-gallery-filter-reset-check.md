# Gallery filter reset check

Confirm gallery filters can be reset after switching wallets or networks.

Filter reset should restore the default collection view without dropping the connected wallet state.

Verification evidence: record the before and after filter values in the release checklist.
