# Wallet network switch copy

Review copy shown when a wallet is connected to the wrong network.

The message should name the expected network and keep reconnect actions distinct from mint actions.

Verification evidence: test the mismatch state with at least one supported wallet provider.
