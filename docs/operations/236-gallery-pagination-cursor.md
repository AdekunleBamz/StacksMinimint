# Gallery pagination cursor

Gallery smoke checks should verify pagination keeps the cursor stable after applying filters.

If a cursor issue appears, include filter values without private wallet labels.
