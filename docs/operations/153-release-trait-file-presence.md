# Release trait file presence

Release packaging should include the local trait files needed for Clarinet compatibility.

Do not rely on a developer machine's ignored deployment plan to hide missing trait contracts.

Run a clean contract check after deleting generated caches when trait files change.
