# Preview Build Artifact

Preview build reviews should verify generated artifacts and environment labels are clean before sharing deployment evidence.

## Checks
- Rebuild after contract alias or environment changes.
- Keep generated caches, local wallets, and secret files out of release screenshots.
- Record the preview URL and network label in the release note.
