# Collection metadata snapshot note

Snapshot collection metadata before release so minted token previews can be compared against the intended collection state.

## Checklist

- Record collection name, symbol, and base metadata URI.
- Confirm the snapshot matches the frontend preview before mint smoke tests.
