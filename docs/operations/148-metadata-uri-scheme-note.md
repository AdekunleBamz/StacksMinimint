# Metadata URI scheme note

Mint QA should cover `https://`, `ipfs://`, and rejected script-like URI inputs.

Record which gateway is used when an IPFS URI needs browser preview support.

This keeps metadata troubleshooting separate from mint transaction failures.
