# Mint Network Env Parity

## Summary
Mint release prep should confirm frontend network variables match the intended contract deployment.

## Checks
- Compare `VITE_STX_NETWORK` with contract address prefixes.
- Keep preview-only overrides out of production notes.
- Document intentional network differences before smoke testing.
