# Wallet Reconnect After Network Switch

When a tester changes wallet network during a mint flow, disconnect and reconnect before submitting another transaction.

Verification: capture the app network badge, wallet network selector, and next transaction hash in the release ticket.
