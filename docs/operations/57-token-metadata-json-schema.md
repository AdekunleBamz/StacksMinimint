# Token metadata JSON schema

Keep the expected token metadata keys documented for release review.

Required fields should stay separate from optional display fields so malformed metadata is easier to triage.

Verification evidence: validate one freshly minted token metadata payload before publishing release notes.
