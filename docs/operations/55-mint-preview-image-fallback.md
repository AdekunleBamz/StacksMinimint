# Mint preview image fallback

Document the fallback behavior for NFT previews when image metadata is slow or unavailable.

Keep the token name, collection, and pending state visible even when the image cannot load.

Verification evidence: capture one successful image load and one fallback preview during release smoke.
