# This file contains git commit SHAs that should be ignored by git blame.
# Useful for skipping formatting commits, large refactors, etc.
# 
# To use this file:
# git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# Format: One commit SHA per line, optionally followed by a comment.