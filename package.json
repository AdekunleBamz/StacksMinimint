{
  "name": "stacksminimint",
  "version": "1.0.0",
  "description": "Modular NFT minting and rewards protocol on Stacks with Clarity contracts and a React frontend",
  "author": "StacksMinimint Contributors",
  "main": "index.js",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "contracts:check": "clarinet check",
    "contracts:console": "clarinet console",
    "check": "npm run contracts:check && npm run test",
    "verify:local": "npm run check && npm run frontend:test",
    "frontend:install": "npm --prefix frontend ci",
    "frontend:dev": "npm --prefix frontend run dev",
    "frontend:build": "npm --prefix frontend run build",
    "build": "npm run frontend:build",
    "frontend:preview": "npm --prefix frontend run preview",
    "frontend:check": "npm --prefix frontend run check",
    "frontend:test": "npm --prefix frontend run test",
    "frontend:test:watch": "npm --prefix frontend run test:watch",
    "frontend:smoke": "npm --prefix frontend run test -- src/hooks/useStacksContract-parseUint-clamps-negative.test.js src/hooks/useStacksWallet-getStacksAddress-trims-value.test.js",
    "check:fast": "npm run contracts:check && npm run frontend:smoke",
    "scripts:install": "[ -f scripts/package.json ] && npm --prefix scripts install || (echo \"scripts/package.json is missing. Add the scripts package before running this command.\" && exit 1)",
    "scripts:wallets": "[ -f scripts/package.json ] && npm --prefix scripts run generate:wallets || (echo \"scripts/package.json is missing. Add the scripts package before running this command.\" && exit 1)",
    "scripts:distribute": "[ -f scripts/package.json ] && npm --prefix scripts run distribute:stx || (echo \"scripts/package.json is missing. Add the scripts package before running this command.\" && exit 1)",
    "scripts:interact": "[ -f scripts/package.json ] && npm --prefix scripts run interact:core || (echo \"scripts/package.json is missing. Add the scripts package before running this command.\" && exit 1)",
    "scripts:interact:token": "[ -f scripts/package.json ] && npm --prefix scripts run interact:token || (echo \"scripts/package.json is missing. Add the scripts package before running this command.\" && exit 1)",
    "scripts:consolidate": "[ -f scripts/package.json ] && npm --prefix scripts run consolidate || (echo \"scripts/package.json is missing. Add the scripts package before running this command.\" && exit 1)"
  },
  "keywords": [
    "stacks",
    "clarity",
    "smart-contracts",
    "nft",
    "web3",
    "bitcoin"
  ],
  "dependencies": {
    "@stacks/network": "^7.3.1",
    "@stacks/transactions": "^7.3.1",
    "@stacks/wallet-sdk": "^7.2.0"
  },
  "devDependencies": {
    "@stacks/clarinet-sdk": "3.15.1",
    "vite": "^5.0.0",
    "vitest": "^1.0.0",
    "vitest-environment-clarinet": "^3.0.2"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/AdekunleBamz/StacksMinimint.git"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "type": "module",
  "bugs": {
    "url": "https://github.com/AdekunleBamz/StacksMinimint/issues"
  },
  "homepage": "https://github.com/AdekunleBamz/StacksMinimint#readme"
}
